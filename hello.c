// Importando a bibliteca padrão de entrada e saída de dados
// std(standard) i(input) o(output)
#include <stdio.h>
// Início do programa.
int main(){
// Comando para exibir hello na tela.
// O comando printf só executa porque você fez a importação
// da biblioteca stdio.
    printf("Hello, World\n");
// O comando abaixo informa ao compilador que o programa acabou
return 0;
}